# Dashboard screenshots

Drop a PNG named `dashboard.png` here. The README links to it.

Easiest way: start the dashboard (`scripts\start_dashboard.bat`), open
http://localhost:8000, hit `Win` + `Shift` + `S`, capture the page, paste into
Paint, save here as `dashboard.png`.
